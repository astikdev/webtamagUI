import { H3, View } from 'tamagui';

const IssuesList = () => {
  // router.navigate(
  //   Routes.APP_DASHBOARD_CORE_ISSUES_DETAILS.replace(':id', issue.slug),
  // );

  return (
    <View f={1} p="$4">
      <H3>Issue List screen</H3>
    </View>
  );
};

export default IssuesList;
